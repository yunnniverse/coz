apiVersion: batch/v1
kind: Job
metadata:
  name: counter-job
spec:
  template:
    spec:
      containers:
      - name: counter
        image: yunniverse/counter:0.0
      restartPolicy: Never
  backoffLimit: 0

